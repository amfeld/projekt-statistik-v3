<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Menu items loaded after views to ensure account module is fully loaded -->
    <data noupdate="0">
        <!-- Main menu under Accounting -->
        <record id="menu_project_analytics_main" model="ir.ui.menu">
            <field name="name">Projekt Statistik</field>
            <field name="parent_id" ref="project.menu_main_pm"/>
            <field name="sequence">25</field>
            <field name="groups_id" eval="[(4, ref('project.group_project_user'))]"/>
        </record>

        <!-- Dashboard submenu -->
        <record id="menu_project_analytics_dashboard" model="ir.ui.menu">
            <field name="name">Dashboard</field>
            <field name="parent_id" ref="menu_project_analytics_main"/>
            <field name="action" ref="action_project_analytics_report"/>
            <field name="sequence">1</field>
            <field name="groups_id" eval="[(4, ref('project.group_project_user'))]"/>
        </record>
    </data>
</odoo>